# Copy to .local/git.env (never commit that file) if GitHub push needs a token.
# Use a fine-grained token with Contents: Read and write for this repository.
GITHUB_TOKEN=github_pat_REPLACE_WITH_YOUR_TOKEN
